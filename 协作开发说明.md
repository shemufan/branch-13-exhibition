# 计算机学院十三支部对标项目协作开发说明

本文档用于统一页面负责人的开发方式，避免页面风格不一致、文件路径混乱、多人协作时互相覆盖代码。

## 1. 项目结构

```text
.
├─ index.html
├─ pages/
│  ├─ school-history.html
│  ├─ college-history.html
│  └─ symposium.html
├─ assets/
│  ├─ css/
│  │  ├─ base.css
│  │  ├─ components.css
│  │  └─ pages.css
│  ├─ js/
│  │  └─ main.js
│  ├─ images/
│  │  ├─ school/
│  │  ├─ college/
│  │  └─ symposium/
│  └─ documents/
│     ├─ school/
│     ├─ college/
│     └─ symposium/
└─ README.md
```

## 2. 分工边界

每位同学只修改自己负责的页面和对应资源目录。

| 负责人 | 页面文件 | 图片目录 | 文档目录 |
| --- | --- | --- | --- |
| 校史负责人 | `pages/school-history.html` | `assets/images/school/` | `assets/documents/school/` |
| 院史负责人 | `pages/college-history.html` | `assets/images/college/` | `assets/documents/college/` |
| 座谈会负责人 | `pages/symposium.html` | `assets/images/symposium/` | `assets/documents/symposium/` |

以下为公共文件：

```text
index.html
assets/css/base.css
assets/css/components.css
assets/css/pages.css
assets/js/main.js
```

## 3. HTML 编辑规范

每个子页面已经有统一结构：

```html
<header class="site-header">
  <!-- 顶部导航 -->
</header>

<main class="subpage-main">
  <!-- 主要内容写在这里 -->
</main>

<footer class="site-footer">
  <!-- 页脚 -->
</footer>
```

请主要修改 `<main class="subpage-main">` 中的内容，不要删除顶部导航和页脚。

推荐页面内容结构：

```html
<section class="section">
  <div class="container">
    <div class="section-heading">
      <p class="section-kicker">Section Title</p>
      <h2>模块标题</h2>
      <p>模块简介文字。</p>
    </div>

    <!-- 这里放具体内容 -->
  </div>
</section>
```

## 4. CSS 使用规范

项目已经提供统一 CSS，不建议每个页面单独写大量内联样式。

三个 CSS 文件作用如下：

| 文件 | 用途 |
| --- | --- |
| `assets/css/base.css` | 全局颜色、字体、基础排版、页面宽度 |
| `assets/css/components.css` | 导航栏、按钮、卡片、页脚、返回顶部 |
| `assets/css/pages.css` | 首页和子页面布局 |

可以直接使用的常用类：

| 类名 | 作用 |
| --- | --- |
| `container` | 限制内容最大宽度，并自动居中 |
| `section` | 标准页面区块，上下留白统一 |
| `section-heading` | 模块标题区域 |
| `section-kicker` | 小标题/英文标签 |
| `button` | 通用按钮基础样式 |
| `button-primary` | 金色主按钮 |
| `button-outline` | 描边按钮 |
| `feature-card` | 首页使用的入口卡片样式 |
| `content-placeholder` | 内容卡片容器 |
| `side-note` | 侧边说明卡片 |

如果需要新增样式，添加到 `assets/css/pages.css`，并使用有意义的类名，例如：

```css
.history-timeline {
  display: grid;
  gap: 18px;
}
```

保证统一风格

## 5. 图片使用规范

图片按页面分类存放：

```text
assets/images/school/
assets/images/college/
assets/images/symposium/
```

子页面引用图片时，路径要从 `pages/` 返回上一级：

```html
<img src="../assets/images/school/example.jpg" alt="校史图片说明">
```

首页引用图片时：

```html
<img src="assets/images/school/example.jpg" alt="校史图片说明">
```

图片命名建议使用英文、数字和短横线：

```text
school-gate.jpg
history-1958.jpg
symposium-scene-01.jpg
```

每张图片都要写 `alt`，方便页面无障碍和维护。

## 6. 文档资料使用规范

资料文件统一放在：

```text
assets/documents/school/
assets/documents/college/
assets/documents/symposium/
```

### 6.1 PDF 优先

如果资料是用于页面展示，优先转换为 PDF，便于预览。

如：

```text
校史资料.pdf
学院介绍.pdf
座谈会纪要.pdf
```

### 6.2 Word 文档作为附件

`docx` 文件可以保留作为下载附件，不建议直接嵌入页面。

示例：

```html
<a class="button button-outline" href="../assets/documents/college/college-introduction.docx" download>
  下载学院介绍 Word 文档
</a>
```

### 6.3 PDF 预览写法

如果需要在页面里直接展示 PDF：

```html
<iframe
  class="document-viewer"
  src="../assets/documents/school/school-history.pdf"
  title="校史资料 PDF 预览">
</iframe>
```

### 6.4 资料链接写法

PDF 打开：

```html
<a href="../assets/documents/school/school-history.pdf" target="_blank">
  查看校史资料 PDF
</a>
```

PDF 下载：

```html
<a href="../assets/documents/school/school-history.pdf" download>
  下载校史资料 PDF
</a>
```

### 6.5. 文件命名规范

推荐使用英文小写、数字和短横线：

```text
school-history.pdf
college-introduction.pdf
symposium-record-01.pdf
history-photo-01.jpg
```

## 7. 提交前检查

每次完成自己的页面后，请至少检查以下内容：

- 页面能在浏览器中正常打开。
- 顶部导航可以跳转到首页、校史、院史、座谈会记录。
- 图片能正常显示，没有裂图。
- PDF 或 Word 链接能正常打开或下载。
- 页面在窄屏下没有明显文字溢出或内容重叠。
- 没有删除公共导航、公共 CSS、公共 JS。
- 文件路径使用相对路径，没有使用自己电脑上的绝对路径。

不要出现绝对路径：

```html
<img src="C:\Users\某某\Desktop\图片.jpg">
```

应该使用：

```html
<img src="../assets/images/school/history-photo-01.jpg" alt="历史照片">
```

## 8. 协作流程

1. 先把资料整理到自己负责的 `assets/images/` 和 `assets/documents/` 目录。
2. 将 Word、PPT 等展示资料尽量导出为 PDF。
3. 只修改自己负责的 HTML 页面。
4. 建议不要直接在 main 分支上修改 先建立新分支后续再合并。
5. 完成后本地打开页面检查跳转、图片和资料链接。
